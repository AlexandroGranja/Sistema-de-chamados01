# Arquivo histórico

Esta pasta guarda cópias antigas ou duplicadas do projeto, **fora do fluxo operacional**.

## Conteúdo

| Pasta | Motivo | App oficial |
|-------|--------|-------------|
| `Gerenciamento de Telefone/` | Cópia espelhada do app Streamlit (Fase A — maio/2026) | Raiz do repositório (`app.py`, `src/`) |

Não execute `ativador.bat` nem edite código aqui. Use sempre a raiz do monorepo.
